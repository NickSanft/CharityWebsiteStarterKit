// The builder page is defined in the parent repo at src/app/builder/page.tsx.
// This file simply re-exports it so a static export emits the same UI at "/".
export { default } from '@/app/builder/page';
